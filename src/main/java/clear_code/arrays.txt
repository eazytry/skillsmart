1. В NativeCache можно обойтись ArrayList (Java) вместо массива
2. В HashTable можно обойтись ArrayList (Java) вместо массива slots
3. В методе обработки участников сделки, использовался массив, где первым всегда был заемщик
  и к этому условию было привязана дальнейшая логика (arr[0] - магическое число). Заменил массив листом, который принимает
  в себя участников сделки.
4. В методе поиска самого дешевого билета на концерт (двумерный массив с рядами и местами) добавил проверки допустимости
  индекса
5. Заменил массив со строками (коды результатов скоринга) на лист