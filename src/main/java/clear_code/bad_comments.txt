1. }
           // создаём массив кучи HeapArray из заданного
           // размер массива выбираем на основе глубины depth
           // ...
  Избыточный комментарий в MakeHeap методе в Heap
2. // если элемент уже был в дереве
               if (Tree[i] == key) {
  Избыточный комментарий в aBST. По условию и так все понятно.
3. }
           // Если в массиве не оказалось места, то возвращаем -1
           return -1;
  Поменял бормочущий комментарий "Места нет" на этот в методе addKey в aBST. Не стал убирать, так как может возникнуть ситуация,
  когда надо будет просто понять в каких случаях возвращается -1 и комментарий может сэкономить время.
4. if (nodeToDelete.LeftChild != null && nodeToDelete.RightChild != null) {
                   // случай когда у удаляемого узла есть два потомка
  Комментарий в BST в методе DeleteNodeByKey стал не нужен
5. // Заменяем потомков кандидата на потомков удаляемого узла
    candidate.LeftChild = nodeToDelete.LeftChild;
    candidate.RightChild = nodeToDelete.RightChild;
  Удалил избыточный/шумный комментарий в методе DeleteNodeByKey в BST
6. // Если удаляем корень = заменяем ссылку в дереве на корень
   if (nodeToDelete == Root) {
     Root = candidate;
   }
  Удалил избыточный/шумный комментарий в методе DeleteNodeByKey в BST
7.   // Если удаляем не корень, то заменяем потомка родителя удаляемого узла на кандидата
     if (nodeToDelete.Parent != null) {
       if (isChildLeft(nodeToDelete.Parent, nodeToDelete)) {
         nodeToDelete.Parent.LeftChild = candidate;
       } else {
         nodeToDelete.Parent.RightChild = candidate;
       }
     }
  Удалил избыточный/шумный комментарий в методе DeleteNodeByKey в BST
8.// Заменяем родителя кандидата на родителя удаляемого узла
  candidate.Parent = nodeToDelete.Parent;
  Удалил избыточный/шумный комментарий в методе DeleteNodeByKey в BST
9.   else {
                return false; // если узел не найден
     }
   Этот блок стоял в конце метода DeleteNodeByKey в BST. Я убрал else и написал более подробный комментарий. Убирать не
   стал тк возможна ситуация, когда разработчик захочет быстро понять почему может вернутся false из метода.
10.  // автоматическая вставка value
     // в нужную позицию
  Удалил шумный комментарий после фигурной скобки в методе add в BST
11.  // добавляем новый элемент key в кучу и перестраиваем её
     return false; // если куча вся заполнена
     Удалил шумны (1) и комментарий после фигурной скобки (2) в методе add в LinkedList2
12. Удалил шумные комментарии в BloomFilter
13. Заменил бормочущий комментарий в FindKeyIndex в aBST
14. Удалил не локльный комментарий возле переменной root в SimpleTree и перенес его в конструктор
15. Удалил шумные и избыточные комментарии в возле переменных в SimpleTree